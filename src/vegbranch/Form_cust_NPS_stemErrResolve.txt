Option Compare Database
Option Explicit


Private Sub Command25_Click()
  DoCmd.OpenForm "cust_NPS_sub_errStems"
End Sub

Private Sub errorID_Enter()
  Me.Recalc
End Sub

Private Sub errorID_Exit(Cancel As Integer)
  Me.Recalc
End Sub

Private Sub Form_Load()
  If Len(Me.OpenArgs) > 0 And Me.OpenArgs <> 0 Then
    Me.errorID = Me.OpenArgs
    Me.Recalc
  End If
End Sub


