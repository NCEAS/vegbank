'$Author: farrell $'
'$Date: 2003-02-03 19:37:20 $'
'$Revision: 1.9 $'
THIS FILE DESCRIBES THE SETUP AND MAINTAINENCE OF A VEGBANK SYSTEM

REQUIRED JAR FILES
-----------------
the following jar files are required in the servlet container

vegcommunity.jar
planttaxonomy.jar
jdbc7.0-1.2.jar
web.xml
utilities.jar
servlet.jar
multipart.jar
frameworkservlet-config.xml
datatranslator.jar
database_access.jar
ant.jar
vegbankquery.jar
usermanagement.jar
stylesheet-generator.jar
frameworkservletmodule.jar
datafile-exchange.jar
authservletmodule.jar
xalan_1_2_2.jar
xmlresource.jar
xml-apis.jar
xerces_1_4.jar

OTHER REQUIRED SERVLET FILES
----------------------------
/vegclass/veg_plot/src/utilities/coord_transform.sh

PROPERTIES FILES:
----------------
./veg_plot/exp/servlet/build/authentication.properties -- edit the name of the server



DATABASE SETUP:
---------------

Vegclass has three separate database parts for Plots, Plant Taxonomy and Plant 
Community. There is also a Framework database for shared resources like users.


Initializing PostgresSQL:

Install PostgresSQL on the system ... 

# Associate a file with the database ( e.g /home/username/pgdata )
initdb databasefile

# Start database ( with debuging output )
postmaster -d2 -i -D databasefile 

# Create databases for "plots_dev, plants_dev, framework and communities_dev"
createdb "name of database" #repeat for each 

# Create the database user 'datauser' as follows:
> createuser  datauser

# Run create table scripts for each database ( in each modules sql directory)
> psql plots_dev < veg_plot/sql/create-vegbank-plots2002.sql  
> psql plots_dev < veg_plot/sql/makePlotSummaryTables_postgres.sql

> psql plants_dev < veg_taxa/sql/createPlantTaxonDb_pg.sql  

> psql communities_dev < veg_community/sql/community_implementation.sql  
> psql communities_dev < veg_community/sql/createCommunitySummary_pg.sql  
> psql communities_dev < veg_community/sql/createEcoartTables_pg.sql   

> psql framework < ./veg_plot/sql/create_veguser_tables.sql

# Run the deNormalization scripts
> psql plants_dev < veg_taxa/sql/denormPlantTaxonDb_pg.sql

# Edit the pg_hba.conf file so that connections may be made to the database 
# add the following:
local      all                                               trust
host       all  127.0.0.1                 255.255.255.255    trust
#host       all  ip of RMI server etc.    255.255.255.255    trust

# GRASS GIS SETUP

This is open source program that is used by vegbank to work with geographic
position data.
The programs homepage is "http://www3.baylor.edu/grass/"

 
