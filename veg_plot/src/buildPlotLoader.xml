<?xml version="1.0"?>
<!-- 
  * build.xml
  *
  *      Authors: John Harris
  *    Copyright: 2000 Regents of the University of California and the
  *               National Center for Ecological Analysis and Synthesis
  *  Build file for the Ant cross-platform build system for metacat
  *  See http://jakarta.apache.org for details on Ant  
  *  
  * This build file will build and insert an xml document, containing plot data,
  * into a working version of the plots database
  * usage: ant [compile|jar|install|jdoc]
--> 
<project name="plotLoader" default="dist" basedir=".">
	<target name="init">
	<tstamp/>
        <property name="current" value="./" />
	<property name="build" value="build" />
	<property name="dist" value="dist" />
	<property name="release" value="development"/>
	<property name="src" value="./plotLoader"/>
	<property name="xmlFiles" value="../xml"/>
	<filter token="dist" value="dist"/>
	<filter token="version" value="development"/>
	
	<property name="oracle_home" 
                value="/oracle01/app/oracle/product/8.1.6" />
	<property name="toolsdir" 
                value="/usr/local/devtools" />
	<property name="localToolsDir" 
                value="/home/computer/harris/tools" />	
	<property name="xmlp" 
                value="${localToolsDir}/xmlTools/xalan.jar:${localToolsDir}/xmlTools/xerces.jar:" />
	<property name="jdbc" 
                value="${oracle_home}/jdbc/lib/classes111.zip" />
	<property name="cpath" 
                value="${xmlp}:${jdbc}:${jserv}:${jsdk}:${build}:${localToolsDir}:./"/>
</target>

<target name="prepare" depends="init">
	<mkdir dir="${build}" />
	<mkdir dir="${dist}" />
	
	<copydir src="${src}" 
               dest="${build}" 
               filtering="yes"
               includes="dbConnect.java plotWriter.java dbLoader.java utility.java transformXML.java artificialPlotExample.xml plot2DBaddress.xsl vegPlot.dtd"
               excludes="**/CVS* "/>

        <copydir src="${xmlFiles}" 
               dest="${build}" 
               filtering="yes"
               includes="artificialPlotExample.xml plot2DBaddress.xsl vegPlot.dtd"
               excludes="**/CVS* "/>
	 
        <copydir src="${current}" 
               dest="${build}" 
               filtering="yes"
               includes="database.properties"
               excludes="**/CVS* "/> 
	
        </target>	
	
<target name="compile" depends="prepare">
	<javac srcdir="${build}" 
	classpath="${cpath}"
	includes="**.java  " 
	destdir="${build}"
	/>
	</target>	
	
<target name="dist" depends="compile">
	<mkdir dir="${dist}/lib" />
	<deltree dir="${dist}" />
	
	<!-- run the plots data loader-->
	<java classname="dbLoader" 
	classpath="${cpath}"
	args="${build}/artificialPlotExample.xml ${build}/plot2DBaddress.xsl insert"
	fork="yes"
	/>
</target>		



</project>
